# Room Remote Control

Home Assistant custom integration for Zigbee2MQTT room remotes.

Files in this repository create a HACS custom integration under `custom_components/room_remote_control`.

Example configuration is in `examples/alisa.yaml`.
